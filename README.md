# Notas:
Este es mi primer programa en Node

```
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base para la tabla de multiplicar     [number] [required]
  -l, --listar   Visualiza la impresión de la tabla   [boolean] [default: false]
  -h, --hasta    La tabla hasta donde se realiza          [number] [default: 10]
```